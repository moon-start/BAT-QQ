language: python
python:
  - 3.7.5
before_install:
  - python --version
  - pip install -U pip
  - git remote -v
  - pip install -r requirements.txt
install:
  - pip install -r requirements.txt
script: python3 A.py || python B.py
